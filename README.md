# lb_5

# Лабораторная работа №5: Построение AST и проверка контекстно-зависимых условий

## Цель
Изучить назначение и принципы работы семантического анализатора в структуре компилятора. Освоить методы построения абстрактного синтаксического дерева (AST) и проверки контекстно-зависимых условий (семантических правил) для заданной синтаксической конструкции.

## Постановка задачи
Развить ранее созданный синтаксический анализатор (парсер) до семантического: построить абстрактное синтаксическое дерево (AST) и реализовать проверку контекстно-зависимых условий в соответствии с индивидуальным вариантом курсовой работы.


**Тема:** Парсинг научной нотации в число на языке JavaScript  
**Вариант:** 48  
**Пример:** `let number = parseFloat("3.234e+4");`


## Контекстно-зависимые условия

| Условие | Описание | Пример ошибки | Ожидаемое сообщение |
|--------------|---------------|---------------|---------------|
| Уникальность имен | Повторное объявление идентификатора в пределах одной программы запрещено | let a = parseFloat('1'); let a = parseFloat('2'); | СЕМАНТИКА: Идентификатор 'a' уже объявлен ранее |
| Допустимые значения (parseFloat) | Проверка, что строковый аргумент преобразуется в число, не выходящее за пределы double | let x = parseFloat('1.8e308'); | СЕМАНТИКА: Преобразованное значение float выходит за границы диапазона double |
| Корректность аргумента | Аргумент parseFloat должен быть строковым литералом | let x = parseFloat(42); | СЕМАНТИКА: parseFloat ожидает строковый аргумент |
| Синтаксическая чистота | "AST строится только для строк, не содержащих ошибок" | let a = pr("3"); | Строка игнорируется (не соответствует структуре научной нотации) |


## Структура AST

AST описание:

ProgramNode — корневой узел дерева. Содержит список всех корректных инструкций программы.

VariableDeclNode — узел объявления переменной. Хранит имя переменной (Id) и узел инициализирующего выражения.

FunctionCallNode — узел вызова функции. Хранит имя функции (parseFloat) и список аргументов.

LiteralNode — листовой узел. Хранит тип (string, number) и значение.


Пример форматированного вывода AST (для строки let a = parseFloat('3.24');):

```
Program
└── VariableDeclNode (id: a)
    └── FunctionCallNode (name: parseFloat)
        └── LiteralNode (Строковая константа: '3.24')
```
